# FitPro 测试指南

> 本指南提供从 Phase 1 到 Phase 2.4（当前完成阶段）的所有测试用例，帮助你验证系统功能是否正常工作。

---

## 📋 测试账户信息

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 超级管理员 | `admin` | `admin123` | 种子数据中已预置，可访问所有管理端功能 |
| 示例教练 | `coach01` | `coach123` | 种子数据中已预置，角色为 COACH |
| 新注册会员 | 任意 | 任意 | 通过注册页面创建，角色默认为 MEMBER |

**注意**：所有密码在数据库中均使用 BCrypt 加密存储。

---

## 🛠️ 测试环境准备

### 1. 数据库与 Redis
- **MySQL**: 确保本地 MySQL 服务运行在 `localhost:3306`
  - 数据库名: `fitpro`
  - 用户名: `root`
  - 密码: `Nan123123`
  - 可通过 `all_tables.sql` 一键导入（已包含 19 张表 + 种子数据）
- **Redis**: 确保本地 Redis 服务运行在 `localhost:6379`，无需密码

### 2. 后端服务
- 启动命令: `mvn spring-boot:run`（在 `fitness-backend/` 目录下执行）
- 服务地址: `http://localhost:8080`
- API 文档: `http://localhost:8080/doc.html`（Knife4j 接口文档）

### 3. 前端服务
- 启动命令: `npm run dev`（在 `fitness-frontend/` 目录下执行）
- 访问地址: `http://localhost:5173`
- 开发代理: 前端 `/api` 请求自动转发到 `http://localhost:8080`

### 4. 工具准备
- **Knife4j 接口文档**：用于手动测试后端 API
- **浏览器开发者工具**（F12）：查看网络请求、控制台输出
- **Postman/Insomnia**（可选）：用于更复杂的 API 测试

---

## ✅ Phase 1 测试用例

### 1.1 健康检查接口
**测试目的**：验证前后端基本连通性。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 启动后端服务（`mvn spring-boot:run`） | 控制台显示 Spring Boot 启动成功，无报错 |
| 2 | 浏览器访问 `http://localhost:8080/api/health` | 返回 JSON: `{"code":200,"message":"success","data":"ok"}` |
| 3 | 启动前端服务（`npm run dev`） | 控制台显示 Vite 启动成功，访问地址 `http://localhost:5173` |
| 4 | 前端页面打开浏览器开发者工具 → 网络标签 | 能看到对 `/api/health` 的请求，状态 200 |

### 1.2 Knife4j 文档可访问
**测试目的**：验证 API 文档是否正常生成。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 浏览器访问 `http://localhost:8080/doc.html` | 显示 Knife4j 接口文档页面 |
| 2 | 在文档中找到「健康检查」接口 | 可看到 `GET /api/health` 接口描述 |
| 3 | 点击「调试」→「发送请求」 | 返回结果与直接访问相同 |

### 1.3 数据库连接验证
**测试目的**：确保后端能正确连接 MySQL 和 Redis。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 启动后端服务，观察控制台日志 | 看到 `DataSource` 和 `Redis` 连接成功的日志 |
| 2 | 在 MySQL 客户端执行 `SELECT * FROM sys_user WHERE username='admin'` | 返回一行管理员数据，密码为 BCrypt 密文 |
| 3 | 通过 Knife4j 调用任意需要数据库的接口（如注册） | 接口正常响应，无数据库连接错误 |

---

## 🔐 Phase 2 测试用例（认证体系）

### 2.1 用户注册流程
**测试目的**：验证新用户注册功能。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 前端访问 `http://localhost:5173`，点击「注册」链接 | 跳转到注册页面，显示表单 |
| 2 | 填写表单：用户名 `testuser`、密码 `test123`、确认密码 `test123`、昵称 `测试用户`、手机号 `13800138000`，勾选协议 | 表单验证通过，无红色错误提示 |
| 3 | 点击「注册」按钮 | 显示加载状态，成功后自动跳转到对应角色页面（MEMBER → `/app/profile`） |
| 4 | 查看浏览器开发者工具 → 网络标签 | 看到 `POST /api/auth/register` 请求成功（200），响应包含 `accessToken` 和 `refreshToken` |
| 5 | 在 MySQL 中查询 `SELECT * FROM sys_user WHERE username='testuser'` | 新增一条用户记录，密码为 BCrypt 密文，角色为 `MEMBER` |

### 2.2 用户登录流程
**测试目的**：验证用户登录功能。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 前端访问 `http://localhost:5173`（或点击登出后） | 显示登录页面 |
| 2 | 使用超级管理员账号登录：用户名 `admin`、密码 `admin123` | 登录成功，跳转到 `/admin/dashboard` |
| 3 | 使用新注册的账号登录：用户名 `testuser`、密码 `test123` | 登录成功，跳转到 `/app/profile` |
| 4 | 使用错误密码登录：用户名 `admin`、密码 `wrong` | 登录失败，显示「用户名或密码错误」提示 |
| 5 | 查看浏览器 LocalStorage | 看到 `token` 和 `refreshToken` 已保存 |

### 2.3 Token 验证与用户信息获取
**测试目的**：验证 Token 有效性及获取当前用户信息。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录成功后，打开浏览器开发者工具 → 控制台 | 无认证相关的错误日志 |
| 2 | 在 Knife4j 中调用 `GET /api/auth/me`，在请求头添加 `Authorization: Bearer {你的accessToken}` | 返回当前用户的完整信息（id、username、nickname、role 等） |
| 3 | 在前端页面刷新（F5） | 页面正常加载，不会跳回登录页（Token 自动恢复） |
| 4 | 手动删除 LocalStorage 中的 `token`，然后刷新页面 | 跳转回登录页面（无 Token 被拦截） |

### 2.4 角色权限验证
**测试目的**：验证不同角色只能访问对应路由。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 使用 `admin`（SUPER_ADMIN）登录 | 成功访问 `/admin/dashboard`，左侧显示完整管理菜单 |
| 2 | 在浏览器地址栏手动输入 `http://localhost:5173/app/profile` | 仍停留在管理端页面（不会跳转，因为角色匹配管理端） |
| 3 | 使用 `testuser`（MEMBER）登录 | 成功访问 `/app/profile`，底部显示会员端 TabBar |
| 4 | 在浏览器地址栏手动输入 `http://localhost:5173/admin/dashboard` | 跳转到 403 无权限页面，显示「您没有权限访问此页面」 |
| 5 | 使用 `coach01`（COACH）登录 | 可根据路由配置测试教练端权限（目前教练与管理员权限类似） |

### 2.5 Token 自动刷新机制
**测试目的**：验证 Access Token 过期后能自动用 Refresh Token 刷新。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后，打开浏览器开发者工具 → 网络标签 | 正常请求 |
| 2 | 手动将 LocalStorage 中的 `token` 改为一个无效字符串（模拟过期） | - |
| 3 | 在前端页面触发一个 API 请求（如刷新页面） | 看到连续两个请求：<br>1. 原请求返回 401<br>2. 自动发送 `POST /api/auth/refresh` 获取新 Token<br>3. 用新 Token 重试原请求成功 |
| 4 | 查看 LocalStorage | `token` 已更新为新值 |
| 5 | 将 `refreshToken` 也改为无效字符串，重复步骤 3 | 自动跳转到登录页面（Refresh Token 也失效） |

### 2.6 用户登出流程
**测试目的**：验证用户登出功能。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后，在前端找到登出按钮（通常在右上角用户菜单） | 点击登出 |
| 2 | 观察网络请求 | 看到 `POST /api/auth/logout` 请求成功 |
| 3 | 检查 LocalStorage | `token` 和 `refreshToken` 已被清除 |
| 4 | 页面跳转 | 自动跳转到登录页面 |
| 5 | 尝试访问需要认证的页面（如 `/app/profile`） | 被路由守卫拦截，跳转登录页 |




## 👤 Phase 3.1 测试用例（用户模块）

### 3.1.1 用户管理接口测试（管理端）
**测试目的**：验证超级管理员对用户列表、详情、状态切换的管理功能。

| 步骤 | 操作 | 预期结果 | 实际结果 | 修复 |
|------|------|----------|----------|------|
| 1 | 使用 `admin` 账号登录，进入管理端 | 登录成功，显示管理端仪表盘 | | |
| 2 | 在 Knife4j 中测试 `GET /api/users`（管理端列表） | 返回分页用户列表，包含所有用户信息（无密码） | | |
| 3 | 在 Knife4j 中添加查询参数：`keyword=admin` | 只返回用户名/昵称/手机号包含 "admin" 的用户 | | |
| 4 | 在 Knife4j 中添加查询参数：`role=SUPER_ADMIN` | 只返回角色为超级管理员的用户 | | |
| 5 | 在 Knife4j 中添加查询参数：`status=0` | 只返回状态为正常的用户 | | |
| 6 | 在 Knife4j 中测试 `GET /api/users/{id}`（用户详情） | 返回指定用户的完整信息（id、username、nickname、phone、email、role、status等） | | |
| 7 | 在 Knife4j 中测试 `PUT /api/users/{id}/status`（切换状态） | 成功切换用户状态（0↔1），返回成功响应 | | |
| 8 | 使用普通会员账号（如 `testuser`）尝试访问 `GET /api/users` | 返回 403 无权限错误（只有 SUPER_ADMIN 能访问） | | |

### 3.1.2 个人资料修改接口测试（用户端）
**测试目的**：验证用户修改自己资料的功能。

| 步骤 | 操作 | 预期结果 | 实际结果 | 修复 |
|------|------|----------|----------|------|
| 1 | 使用 `testuser` 账号登录，进入会员端个人中心 | 登录成功，显示个人中心页面 | √ | |
| 2 | 在 Knife4j 中测试 `PUT /api/users/profile`（修改资料） | 成功更新用户资料，返回成功响应 | 403响应码 | |
| 3 | 测试修改昵称：`{"nickname": "新昵称"}` | 昵称更新成功，后续查询返回新昵称 | 403 | |
| 4 | 测试修改手机号：`{"phone": "13800138001"}` | 手机号更新成功 | 403 | |
| 5 | 测试修改邮箱：`{"email": "new@example.com"}` | 邮箱更新成功 | 403 | |
| 6 | 测试修改性别：`{"gender": 1}`（1=男） | 性别更新成功 | 403 | |
| 7 | 测试修改生日：`{"birthday": "1990-01-01"}` | 生日更新成功 | 403 | |
| 8 | 测试手机号重复：用另一个用户的手机号尝试更新 | 返回错误 "手机号已被其他用户使用" (1102) | 403 | |
| 9 | 测试邮箱重复：用另一个用户的邮箱尝试更新 | 返回错误 "邮箱已被其他用户使用" (1103) | 403 | |

### 3.1.3 密码修改接口测试（用户端）
**测试目的**：验证用户修改密码的功能。

| 步骤 | 操作 | 预期结果 | 实际结果 | 修复 |
|------|------|----------|----------|------|
| 1 | 使用 `testuser` 账号登录 | 登录成功 | √ | |
| 2 | 在 Knife4j 中测试 `PUT /api/users/password`（修改密码） | 成功修改密码，返回成功响应 | | |
| 3 | 测试正确流程：`{"oldPassword": "test123", "newPassword": "newpass123", "confirmPassword": "newpass123"}` | 密码修改成功，自动登出，需用新密码重新登录 | | |
| 4 | 测试原密码错误：`{"oldPassword": "wrong", "newPassword": "newpass123", "confirmPassword": "newpass123"}` | 返回错误 "原密码错误" (1104) | | |
| 5 | 测试新密码不一致：`{"oldPassword": "test123", "newPassword": "newpass123", "confirmPassword": "different"}` | 返回错误 "两次输入的新密码不一致" (1105) | | |
| 6 | 测试密码长度：`{"oldPassword": "test123", "newPassword": "123", "confirmPassword": "123"}` | 返回验证错误 "密码长度在 6 到 20 个字符" | | |

### 3.1.4 前端个人中心页面测试
**测试目的**：验证前端个人中心页面的功能完整性。

| 步骤 | 操作 | 预期结果 | 实际结果 | 修复 |
|------|------|----------|----------|------|
| 1 | 使用会员账号登录，访问 `/app/profile` | 显示个人中心页面，包含「基本信息」和「修改密码」两个标签页 | √ | |
| 2 | 在「基本信息」标签页查看表单 | 显示当前用户的用户名、角色、昵称、手机号、邮箱、性别、生日等信息 | √ | |
| 3 | 修改昵称字段，点击「保存修改」 | 显示加载状态，成功后显示「个人信息更新成功」提示 | √ | |
| 4 | 修改手机号为无效格式（如 123456） | 表单验证显示红色错误提示「手机号格式不正确」 | √ | |
| 5 | 修改邮箱为无效格式（如 not-an-email） | 表单验证显示红色错误提示「邮箱格式不正确」 | √ ||
| 6 | 点击「更换头像」按钮 | 弹出文件选择对话框，可选择图片文件 | √ | |
| 7 | 选择图片文件后 | 头像预览更新为选择的图片（前端预览，实际未上传服务器） | 保存的时候显示服务器错误 | |
| 8 | 切换到「修改密码」标签页 | 显示原密码、新密码、确认密码三个输入框 | √ | |
| 9 | 填写原密码错误，点击「确认修改」 | 显示错误提示「原密码错误」 | √ | |
| 10 | 填写新密码和确认密码不一致 | 显示错误提示「两次输入的密码不一致」 | √ | |
| 11 | 正确填写所有字段，点击「确认修改」 | 显示「密码修改成功，请重新登录」提示，自动跳转到登录页 | √ | |
| 12 | 点击「退出登录」按钮 | 显示确认对话框，确认后清除登录状态，跳转到登录页 | √ | |

### 3.1.5 前端用户管理页面测试（管理端）
**测试目的**：验证管理端用户管理页面的功能完整性。

| 步骤 | 操作 | 预期结果 | 实际结果 | 修复 |
|------|------|----------|----------|------|
| 1 | 使用 `admin` 账号登录，访问用户管理页面（通常在左侧菜单「会员管理」） | 显示用户管理页面，包含搜索栏、用户表格、分页组件 | 没有搜索栏 | |
| 2 | 在搜索栏输入关键词（如 "admin"）点击搜索 | 表格只显示包含该关键词的用户（用户名/昵称/手机号匹配） | 没有搜索栏 | |
| 3 | 在角色下拉框选择「教练」 | 表格只显示角色为 COACH 的用户 | 不能跳转教练页面 | |
| 4 | 在状态下拉框选择「禁用」 | 表格只显示状态为禁用（1）的用户 | 不能跳转 | |
| 5 | 点击「重置」按钮 | 搜索条件清空，显示所有用户 | 不能跳转 | |
| 6 | 在表格中找到某个用户的状态开关 | 开关显示当前状态（正常=蓝色，禁用=灰色） | | |
| 7 | 点击状态开关从正常切换到禁用 | 弹出确认对话框「确定要禁用用户 "XXX" 吗？」 | | |
| 8 | 点击「确定」 | 开关状态改变，显示「状态更新成功」提示 | | |
| 9 | 点击「取消」 | 开关恢复原状态，无状态变更 | | |
| 10 | 点击某个用户的「详情」按钮 | 弹出用户详情弹窗，显示该用户的完整信息 | | |
| 11 | 在详情弹窗中查看信息 | 显示用户ID、用户名、昵称、角色、手机号、邮箱、性别、生日、状态、注册时间、更新时间等 | 不能跳转 | |
| 12 | 点击分页组件的「下一页」或选择每页显示条数 | 表格数据按分页参数刷新 | 不能跳转 | |
| 13 | 使用普通会员账号尝试访问用户管理页面 | 显示 403 无权限页面（路由守卫拦截） | | |

### 3.1.6 权限边界测试
**测试目的**：验证用户只能访问自己权限范围内的数据。

| 步骤 | 操作 | 预期结果 | 实际结果 | 修复 |
|------|------|----------|----------|------|
| 1 | 使用 `testuser`（MEMBER）登录，在 Knife4j 中尝试访问 `GET /api/users`（管理端列表） | 返回 403 无权限错误 | | |
| 2 | 使用 `testuser` 尝试访问 `PUT /api/users/{其他用户ID}/status` | 返回 403 无权限错误 | | |
| 3 | 使用 `testuser` 尝试访问 `GET /api/users/{其他用户ID}`（查看其他用户详情） | 返回 403 无权限错误（只能查看自己的详情） | | |
| 4 | 使用 `testuser` 访问 `GET /api/users/{自己的ID}` | 成功返回自己的用户信息 | | |
| 5 | 使用 `admin` 访问 `GET /api/users/{任意用户ID}` | 成功返回任意用户信息（SUPER_ADMIN 有权限） | | |
| 6 | 使用 `testuser` 修改其他用户的资料 `PUT /api/users/profile`（实际只能改自己的） | 后端通过当前登录用户ID限制，只能修改自己的资料 | | |

